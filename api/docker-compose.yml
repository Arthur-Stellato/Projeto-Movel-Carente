services:
  redis:
    image: redis:alpine
    container_name: redis-movelcarente
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - redis-movelcarente-data:/data

volumes:
  redis-movelcarente-data:
